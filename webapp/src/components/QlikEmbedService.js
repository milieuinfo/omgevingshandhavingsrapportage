//import { setDefaultHostConfig } from '@qlik/embed-web-components';

/*
class QlikEmbedService {
  constructor(config) {
    this.config = {
      host: config.host || '[atonce-msp.eu.qlikcloud.com](https://atonce-msp.eu.qlikcloud.com)',
      clientId: config.clientId,
      redirectUri: config.redirectUri || window.location.origin + '/oauth_callback.html',
      accessTokenStorage: config.accessTokenStorage || 'session',
      autoRedirect: config.autoRedirect ?? true,
    };
  }

  initialize() {
    setDefaultHostConfig({
      host: this.config.host,
      clientId: this.config.clientId,
      redirectUri: this.config.redirectUri,
      accessTokenStorage: this.config.accessTokenStorage,
      autoRedirect: this.config.autoRedirect,
    });
  }
}

export default QlikEmbedService;
*/